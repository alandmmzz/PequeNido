# Copiá este archivo como .env.local y pegá tu connection string de Neon
# (Neon dashboard -> tu proyecto -> Connect -> "Connection string", con sslmode=require)
DATABASE_URL="postgresql://usuario:password@ep-xxxxx.neon.tech/dbname?sslmode=require"

# --- Mercado Pago (Checkout Pro) ---
# Sacalo de https://www.mercadopago.com.ar/developers/panel/app -> tu app -> "Credenciales de prueba"
# (o "Credenciales de producción" cuando esté listo para cobrar de verdad)
MERCADOPAGO_ACCESS_TOKEN="TEST-0000000000000000-000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-000000000"

# Mercado Pago ahora usa el mismo formato de token ("APP_USR-...") tanto para
# credenciales de prueba como reales, así que hace falta esta bandera aparte
# para saber a qué checkout mandar al comprador:
# - "true" mientras estés probando con las credenciales de prueba de tu panel de Mercado Pago
# - "false" (o directamente sacar la variable) cuando pases a las credenciales de producción para cobrar de verdad
MERCADOPAGO_SANDBOX="true"

# Moneda en la que vas a cobrar (todos los precios del catálogo están en UYU).
MERCADOPAGO_CURRENCY="UYU"

# URL pública de tu sitio (sin barra final). Mercado Pago la necesita para
# redirigir de vuelta al comprador y para mandar el webhook de pagos.
# En local, si querés probar el webhook de verdad, usá una URL de ngrok/tunnel.
NEXT_PUBLIC_URL="http://localhost:3000"

# --- Login del panel de administración (/admin), por magic link ---
# El o los emails que pueden entrar al panel (separados por coma si son varios).
ADMIN_EMAIL="tu@email.com"

# Clave secreta larga y random para firmar la sesión. Generála con:
# openssl rand -base64 32
AUTH_SECRET="reemplazar-por-una-clave-random-larga"

# API key de Resend (gratis en resend.com) para mandar el mail con el link de acceso.
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Remitente del mail. Con el dominio de prueba de Resend (onboarding@resend.dev)
# funciona sin configurar nada más; para usar tu propio dominio hay que
# verificarlo en Resend primero.
EMAIL_FROM="Pequenido <onboarding@resend.dev>"

# A quién avisar por mail cuando entra un pedido nuevo (separados por coma
# si son varios). Si no la seteás, se usa ADMIN_EMAIL.
NOTIFICATION_EMAIL="tu@email.com"
